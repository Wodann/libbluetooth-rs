#![cfg(all(unix, target_arch = "x86_64"))]
extern crate libbluetooth;
use std::mem::{align_of, size_of};
#[test]
fn bluetooth() {
    use libbluetooth::bluetooth::*;
    assert_eq!(size_of::<bt_security>(), 2);
    assert_eq!(align_of::<bt_security>(), 1);
    assert_eq!(size_of::<bt_voice>(), 2);
    assert_eq!(align_of::<bt_voice>(), 2);
    assert_eq!(size_of::<bdaddr_t>(), 6);
    assert_eq!(align_of::<bdaddr_t>(), 1);
}
#[test]
fn bnep() {
    use libbluetooth::bnep::*;
    assert_eq!(size_of::<bnep_setup_conn_req>(), 3);
    assert_eq!(align_of::<bnep_setup_conn_req>(), 1);
    assert_eq!(size_of::<bnep_set_filter_req>(), 4);
    assert_eq!(align_of::<bnep_set_filter_req>(), 1);
    assert_eq!(size_of::<bnep_ctrl_cmd_not_understood_cmd>(), 3);
    assert_eq!(align_of::<bnep_ctrl_cmd_not_understood_cmd>(), 1);
    assert_eq!(size_of::<bnep_control_rsp>(), 4);
    assert_eq!(align_of::<bnep_control_rsp>(), 1);
    assert_eq!(size_of::<bnep_ext_hdr>(), 2);
    assert_eq!(align_of::<bnep_ext_hdr>(), 1);
    assert_eq!(size_of::<bnep_connadd_req>(), 28);
    assert_eq!(align_of::<bnep_connadd_req>(), 4);
    assert_eq!(size_of::<bnep_conndel_req>(), 12);
    assert_eq!(align_of::<bnep_conndel_req>(), 4);
    assert_eq!(size_of::<bnep_conninfo>(), 32);
    assert_eq!(align_of::<bnep_conninfo>(), 4);
    assert_eq!(size_of::<bnep_connlist_req>(), 16);
    assert_eq!(align_of::<bnep_connlist_req>(), 8);
}
#[test]
fn cmtp() {
    use libbluetooth::cmtp::*;
    assert_eq!(size_of::<cmtp_connadd_req>(), 8);
    assert_eq!(align_of::<cmtp_connadd_req>(), 4);
    assert_eq!(size_of::<cmtp_conndel_req>(), 12);
    assert_eq!(align_of::<cmtp_conndel_req>(), 4);
    assert_eq!(size_of::<cmtp_conninfo>(), 20);
    assert_eq!(align_of::<cmtp_conninfo>(), 4);
    assert_eq!(size_of::<cmtp_connlist_req>(), 16);
    assert_eq!(align_of::<cmtp_connlist_req>(), 8);
}
#[test]
fn hci() {
    use libbluetooth::hci::*;
    assert_eq!(align_of::<inquiry_cp>(), 1);
    assert_eq!(size_of::<status_bdaddr_rp>(), 7);
    assert_eq!(align_of::<status_bdaddr_rp>(), 1);
    assert_eq!(size_of::<periodic_inquiry_cp>(), 9);
    assert_eq!(align_of::<periodic_inquiry_cp>(), 1);
    assert_eq!(size_of::<create_conn_cp>(), 13);
    assert_eq!(align_of::<create_conn_cp>(), 1);
    assert_eq!(size_of::<disconnect_cp>(), 3);
    assert_eq!(align_of::<disconnect_cp>(), 1);
    assert_eq!(size_of::<add_sco_cp>(), 4);
    assert_eq!(align_of::<add_sco_cp>(), 1);
    assert_eq!(size_of::<create_conn_cancel_cp>(), 6);
    assert_eq!(align_of::<create_conn_cancel_cp>(), 1);
    assert_eq!(size_of::<accept_conn_req_cp>(), 7);
    assert_eq!(align_of::<accept_conn_req_cp>(), 1);
    assert_eq!(size_of::<reject_conn_req_cp>(), 7);
    assert_eq!(align_of::<reject_conn_req_cp>(), 1);
    assert_eq!(size_of::<link_key_reply_cp>(), 22);
    assert_eq!(align_of::<link_key_reply_cp>(), 1);
    assert_eq!(size_of::<pin_code_reply_cp>(), 23);
    assert_eq!(align_of::<pin_code_reply_cp>(), 1);
    assert_eq!(size_of::<set_conn_ptype_cp>(), 4);
    assert_eq!(align_of::<set_conn_ptype_cp>(), 1);
    assert_eq!(size_of::<auth_requested_cp>(), 2);
    assert_eq!(align_of::<auth_requested_cp>(), 1);
    assert_eq!(size_of::<set_conn_encrypt_cp>(), 3);
    assert_eq!(align_of::<set_conn_encrypt_cp>(), 1);
    assert_eq!(size_of::<change_conn_link_key_cp>(), 2);
    assert_eq!(align_of::<change_conn_link_key_cp>(), 1);
    assert_eq!(size_of::<master_link_key_cp>(), 1);
    assert_eq!(align_of::<master_link_key_cp>(), 1);
    assert_eq!(size_of::<remote_name_req_cp>(), 10);
    assert_eq!(align_of::<remote_name_req_cp>(), 1);
    assert_eq!(size_of::<remote_name_req_cancel_cp>(), 6);
    assert_eq!(align_of::<remote_name_req_cancel_cp>(), 1);
    assert_eq!(size_of::<read_remote_features_cp>(), 2);
    assert_eq!(align_of::<read_remote_features_cp>(), 1);
    assert_eq!(size_of::<read_remote_ext_features_cp>(), 3);
    assert_eq!(align_of::<read_remote_ext_features_cp>(), 1);
    assert_eq!(size_of::<read_remote_version_cp>(), 2);
    assert_eq!(align_of::<read_remote_version_cp>(), 1);
    assert_eq!(size_of::<read_clock_offset_cp>(), 2);
    assert_eq!(align_of::<read_clock_offset_cp>(), 1);
    assert_eq!(size_of::<setup_sync_conn_cp>(), 17);
    assert_eq!(align_of::<setup_sync_conn_cp>(), 1);
    assert_eq!(size_of::<accept_sync_conn_req_cp>(), 21);
    assert_eq!(align_of::<accept_sync_conn_req_cp>(), 1);
    assert_eq!(size_of::<reject_sync_conn_req_cp>(), 7);
    assert_eq!(align_of::<reject_sync_conn_req_cp>(), 1);
    assert_eq!(size_of::<io_capability_reply_cp>(), 9);
    assert_eq!(align_of::<io_capability_reply_cp>(), 1);
    assert_eq!(size_of::<user_confirm_reply_cp>(), 6);
    assert_eq!(align_of::<user_confirm_reply_cp>(), 1);
    assert_eq!(size_of::<user_passkey_reply_cp>(), 10);
    assert_eq!(align_of::<user_passkey_reply_cp>(), 1);
    assert_eq!(size_of::<remote_oob_data_reply_cp>(), 38);
    assert_eq!(align_of::<remote_oob_data_reply_cp>(), 1);
    assert_eq!(size_of::<io_capability_neg_reply_cp>(), 7);
    assert_eq!(align_of::<io_capability_neg_reply_cp>(), 1);
    assert_eq!(size_of::<create_physical_link_cp>(), 35);
    assert_eq!(align_of::<create_physical_link_cp>(), 1);
    assert_eq!(size_of::<accept_physical_link_cp>(), 35);
    assert_eq!(align_of::<accept_physical_link_cp>(), 1);
    assert_eq!(size_of::<disconnect_physical_link_cp>(), 2);
    assert_eq!(align_of::<disconnect_physical_link_cp>(), 1);
    assert_eq!(size_of::<create_logical_link_cp>(), 33);
    assert_eq!(align_of::<create_logical_link_cp>(), 1);
    assert_eq!(size_of::<disconnect_logical_link_cp>(), 2);
    assert_eq!(align_of::<disconnect_logical_link_cp>(), 1);
    assert_eq!(size_of::<cancel_logical_link_cp>(), 2);
    assert_eq!(align_of::<cancel_logical_link_cp>(), 1);
    assert_eq!(size_of::<cancel_logical_link_rp>(), 3);
    assert_eq!(align_of::<cancel_logical_link_rp>(), 1);
    assert_eq!(size_of::<hold_mode_cp>(), 6);
    assert_eq!(align_of::<hold_mode_cp>(), 1);
    assert_eq!(size_of::<sniff_mode_cp>(), 10);
    assert_eq!(align_of::<sniff_mode_cp>(), 1);
    assert_eq!(size_of::<exit_sniff_mode_cp>(), 2);
    assert_eq!(align_of::<exit_sniff_mode_cp>(), 1);
    assert_eq!(size_of::<park_mode_cp>(), 6);
    assert_eq!(align_of::<park_mode_cp>(), 1);
    assert_eq!(size_of::<exit_park_mode_cp>(), 2);
    assert_eq!(align_of::<exit_park_mode_cp>(), 1);
    assert_eq!(size_of::<hci_qos>(), 17);
    assert_eq!(align_of::<hci_qos>(), 1);
    assert_eq!(size_of::<qos_setup_cp>(), 20);
    assert_eq!(align_of::<qos_setup_cp>(), 1);
    assert_eq!(size_of::<role_discovery_cp>(), 2);
    assert_eq!(align_of::<role_discovery_cp>(), 1);
    assert_eq!(size_of::<role_discovery_rp>(), 4);
    assert_eq!(align_of::<role_discovery_rp>(), 1);
    assert_eq!(size_of::<switch_role_cp>(), 7);
    assert_eq!(align_of::<switch_role_cp>(), 1);
    assert_eq!(size_of::<read_link_policy_cp>(), 2);
    assert_eq!(align_of::<read_link_policy_cp>(), 1);
    assert_eq!(size_of::<read_link_policy_rp>(), 5);
    assert_eq!(align_of::<read_link_policy_rp>(), 1);
    assert_eq!(size_of::<write_link_policy_cp>(), 4);
    assert_eq!(align_of::<write_link_policy_cp>(), 1);
    assert_eq!(size_of::<write_link_policy_rp>(), 3);
    assert_eq!(align_of::<write_link_policy_rp>(), 1);
    assert_eq!(size_of::<sniff_subrating_cp>(), 8);
    assert_eq!(align_of::<sniff_subrating_cp>(), 1);
    assert_eq!(size_of::<set_event_mask_cp>(), 8);
    assert_eq!(align_of::<set_event_mask_cp>(), 1);
    assert_eq!(size_of::<set_event_flt_cp>(), 2);
    assert_eq!(align_of::<set_event_flt_cp>(), 1);
    assert_eq!(size_of::<read_pin_type_rp>(), 2);
    assert_eq!(align_of::<read_pin_type_rp>(), 1);
    assert_eq!(size_of::<write_pin_type_cp>(), 1);
    assert_eq!(align_of::<write_pin_type_cp>(), 1);
    assert_eq!(size_of::<read_stored_link_key_cp>(), 7);
    assert_eq!(align_of::<read_stored_link_key_cp>(), 1);
    assert_eq!(size_of::<read_stored_link_key_rp>(), 5);
    assert_eq!(align_of::<read_stored_link_key_rp>(), 1);
    assert_eq!(size_of::<write_stored_link_key_cp>(), 1);
    assert_eq!(align_of::<write_stored_link_key_cp>(), 1);
    assert_eq!(size_of::<write_stored_link_key_rp>(), 2);
    assert_eq!(align_of::<write_stored_link_key_rp>(), 1);
    assert_eq!(size_of::<delete_stored_link_key_cp>(), 7);
    assert_eq!(align_of::<delete_stored_link_key_cp>(), 1);
    assert_eq!(size_of::<delete_stored_link_key_rp>(), 3);
    assert_eq!(align_of::<delete_stored_link_key_rp>(), 1);
    assert_eq!(size_of::<change_local_name_cp>(), 248);
    assert_eq!(align_of::<change_local_name_cp>(), 1);
    assert_eq!(size_of::<read_local_name_rp>(), 249);
    assert_eq!(align_of::<read_local_name_rp>(), 1);
    assert_eq!(size_of::<read_conn_accept_timeout_rp>(), 3);
    assert_eq!(align_of::<read_conn_accept_timeout_rp>(), 1);
    assert_eq!(size_of::<write_conn_accept_timeout_cp>(), 2);
    assert_eq!(align_of::<write_conn_accept_timeout_cp>(), 1);
    assert_eq!(size_of::<read_page_timeout_rp>(), 3);
    assert_eq!(align_of::<read_page_timeout_rp>(), 1);
    assert_eq!(size_of::<write_page_timeout_cp>(), 2);
    assert_eq!(align_of::<write_page_timeout_cp>(), 1);
    assert_eq!(size_of::<read_scan_enable_rp>(), 2);
    assert_eq!(align_of::<read_scan_enable_rp>(), 1);
    assert_eq!(size_of::<read_page_activity_rp>(), 5);
    assert_eq!(align_of::<read_page_activity_rp>(), 1);
    assert_eq!(size_of::<write_page_activity_cp>(), 4);
    assert_eq!(align_of::<write_page_activity_cp>(), 1);
    assert_eq!(size_of::<read_inq_activity_rp>(), 5);
    assert_eq!(align_of::<read_inq_activity_rp>(), 1);
    assert_eq!(size_of::<write_inq_activity_cp>(), 4);
    assert_eq!(align_of::<write_inq_activity_cp>(), 1);
    assert_eq!(size_of::<read_class_of_dev_rp>(), 4);
    assert_eq!(align_of::<read_class_of_dev_rp>(), 1);
    assert_eq!(size_of::<write_class_of_dev_cp>(), 3);
    assert_eq!(align_of::<write_class_of_dev_cp>(), 1);
    assert_eq!(size_of::<read_voice_setting_rp>(), 3);
    assert_eq!(align_of::<read_voice_setting_rp>(), 1);
    assert_eq!(size_of::<write_voice_setting_cp>(), 2);
    assert_eq!(align_of::<write_voice_setting_cp>(), 1);
    assert_eq!(size_of::<read_transmit_power_level_cp>(), 3);
    assert_eq!(align_of::<read_transmit_power_level_cp>(), 1);
    assert_eq!(size_of::<read_transmit_power_level_rp>(), 4);
    assert_eq!(align_of::<read_transmit_power_level_rp>(), 1);
    assert_eq!(size_of::<host_buffer_size_cp>(), 7);
    assert_eq!(align_of::<host_buffer_size_cp>(), 1);
    assert_eq!(size_of::<host_num_comp_pkts_cp>(), 1);
    assert_eq!(align_of::<host_num_comp_pkts_cp>(), 1);
    assert_eq!(size_of::<read_link_supervision_timeout_rp>(), 5);
    assert_eq!(align_of::<read_link_supervision_timeout_rp>(), 1);
    assert_eq!(size_of::<write_link_supervision_timeout_cp>(), 4);
    assert_eq!(align_of::<write_link_supervision_timeout_cp>(), 1);
    assert_eq!(size_of::<write_link_supervision_timeout_rp>(), 3);
    assert_eq!(align_of::<write_link_supervision_timeout_rp>(), 1);
    assert_eq!(size_of::<read_current_iac_lap_rp>(), 194);
    assert_eq!(align_of::<read_current_iac_lap_rp>(), 1);
    assert_eq!(size_of::<write_current_iac_lap_cp>(), 193);
    assert_eq!(align_of::<write_current_iac_lap_cp>(), 1);
    assert_eq!(size_of::<set_afh_classification_cp>(), 10);
    assert_eq!(align_of::<set_afh_classification_cp>(), 1);
    assert_eq!(size_of::<set_afh_classification_rp>(), 1);
    assert_eq!(align_of::<set_afh_classification_rp>(), 1);
    assert_eq!(size_of::<read_inquiry_scan_type_rp>(), 2);
    assert_eq!(align_of::<read_inquiry_scan_type_rp>(), 1);
    assert_eq!(size_of::<write_inquiry_scan_type_cp>(), 1);
    assert_eq!(align_of::<write_inquiry_scan_type_cp>(), 1);
    assert_eq!(size_of::<write_inquiry_scan_type_rp>(), 1);
    assert_eq!(align_of::<write_inquiry_scan_type_rp>(), 1);
    assert_eq!(size_of::<read_inquiry_mode_rp>(), 2);
    assert_eq!(align_of::<read_inquiry_mode_rp>(), 1);
    assert_eq!(size_of::<write_inquiry_mode_cp>(), 1);
    assert_eq!(align_of::<write_inquiry_mode_cp>(), 1);
    assert_eq!(size_of::<write_inquiry_mode_rp>(), 1);
    assert_eq!(align_of::<write_inquiry_mode_rp>(), 1);
    assert_eq!(size_of::<read_afh_mode_rp>(), 2);
    assert_eq!(align_of::<read_afh_mode_rp>(), 1);
    assert_eq!(size_of::<write_afh_mode_cp>(), 1);
    assert_eq!(align_of::<write_afh_mode_cp>(), 1);
    assert_eq!(size_of::<write_afh_mode_rp>(), 1);
    assert_eq!(align_of::<write_afh_mode_rp>(), 1);
    assert_eq!(size_of::<read_ext_inquiry_response_rp>(), 242);
    assert_eq!(align_of::<read_ext_inquiry_response_rp>(), 1);
    assert_eq!(size_of::<write_ext_inquiry_response_cp>(), 241);
    assert_eq!(align_of::<write_ext_inquiry_response_cp>(), 1);
    assert_eq!(size_of::<write_ext_inquiry_response_rp>(), 1);
    assert_eq!(align_of::<write_ext_inquiry_response_rp>(), 1);
    assert_eq!(size_of::<refresh_encryption_key_cp>(), 2);
    assert_eq!(align_of::<refresh_encryption_key_cp>(), 1);
    assert_eq!(size_of::<refresh_encryption_key_rp>(), 1);
    assert_eq!(align_of::<refresh_encryption_key_rp>(), 1);
    assert_eq!(size_of::<read_simple_pairing_mode_rp>(), 2);
    assert_eq!(align_of::<read_simple_pairing_mode_rp>(), 1);
    assert_eq!(size_of::<write_simple_pairing_mode_cp>(), 1);
    assert_eq!(align_of::<write_simple_pairing_mode_cp>(), 1);
    assert_eq!(size_of::<write_simple_pairing_mode_rp>(), 1);
    assert_eq!(align_of::<write_simple_pairing_mode_rp>(), 1);
    assert_eq!(size_of::<read_local_oob_data_rp>(), 33);
    assert_eq!(align_of::<read_local_oob_data_rp>(), 1);
    assert_eq!(size_of::<read_inq_response_tx_power_level_rp>(), 2);
    assert_eq!(align_of::<read_inq_response_tx_power_level_rp>(), 1);
    assert_eq!(size_of::<read_inquiry_transmit_power_level_rp>(), 2);
    assert_eq!(align_of::<read_inquiry_transmit_power_level_rp>(), 1);
    assert_eq!(size_of::<write_inquiry_transmit_power_level_cp>(), 1);
    assert_eq!(align_of::<write_inquiry_transmit_power_level_cp>(), 1);
    assert_eq!(size_of::<write_inquiry_transmit_power_level_rp>(), 1);
    assert_eq!(align_of::<write_inquiry_transmit_power_level_rp>(), 1);
    assert_eq!(size_of::<read_default_error_data_reporting_rp>(), 2);
    assert_eq!(align_of::<read_default_error_data_reporting_rp>(), 1);
    assert_eq!(size_of::<write_default_error_data_reporting_cp>(), 1);
    assert_eq!(align_of::<write_default_error_data_reporting_cp>(), 1);
    assert_eq!(size_of::<write_default_error_data_reporting_rp>(), 1);
    assert_eq!(align_of::<write_default_error_data_reporting_rp>(), 1);
    assert_eq!(size_of::<enhanced_flush_cp>(), 3);
    assert_eq!(align_of::<enhanced_flush_cp>(), 1);
    assert_eq!(size_of::<send_keypress_notify_cp>(), 7);
    assert_eq!(align_of::<send_keypress_notify_cp>(), 1);
    assert_eq!(size_of::<send_keypress_notify_rp>(), 1);
    assert_eq!(align_of::<send_keypress_notify_rp>(), 1);
    assert_eq!(size_of::<read_log_link_accept_timeout_rp>(), 3);
    assert_eq!(align_of::<read_log_link_accept_timeout_rp>(), 1);
    assert_eq!(size_of::<write_log_link_accept_timeout_cp>(), 2);
    assert_eq!(align_of::<write_log_link_accept_timeout_cp>(), 1);
    assert_eq!(size_of::<read_enhanced_transmit_power_level_rp>(), 6);
    assert_eq!(align_of::<read_enhanced_transmit_power_level_rp>(), 1);
    assert_eq!(size_of::<read_best_effort_flush_timeout_rp>(), 5);
    assert_eq!(align_of::<read_best_effort_flush_timeout_rp>(), 1);
    assert_eq!(size_of::<write_best_effort_flush_timeout_cp>(), 6);
    assert_eq!(align_of::<write_best_effort_flush_timeout_cp>(), 1);
    assert_eq!(size_of::<write_best_effort_flush_timeout_rp>(), 1);
    assert_eq!(align_of::<write_best_effort_flush_timeout_rp>(), 1);
    assert_eq!(size_of::<read_le_host_supported_rp>(), 3);
    assert_eq!(align_of::<read_le_host_supported_rp>(), 1);
    assert_eq!(size_of::<write_le_host_supported_cp>(), 2);
    assert_eq!(align_of::<write_le_host_supported_cp>(), 1);
    assert_eq!(size_of::<read_local_version_rp>(), 9);
    assert_eq!(align_of::<read_local_version_rp>(), 1);
    assert_eq!(size_of::<read_local_commands_rp>(), 65);
    assert_eq!(align_of::<read_local_commands_rp>(), 1);
    assert_eq!(size_of::<read_local_features_rp>(), 9);
    assert_eq!(align_of::<read_local_features_rp>(), 1);
    assert_eq!(size_of::<read_local_ext_features_cp>(), 1);
    assert_eq!(align_of::<read_local_ext_features_cp>(), 1);
    assert_eq!(size_of::<read_local_ext_features_rp>(), 11);
    assert_eq!(align_of::<read_local_ext_features_rp>(), 1);
    assert_eq!(size_of::<read_buffer_size_rp>(), 8);
    assert_eq!(align_of::<read_buffer_size_rp>(), 1);
    assert_eq!(size_of::<read_bd_addr_rp>(), 7);
    assert_eq!(align_of::<read_bd_addr_rp>(), 1);
    assert_eq!(size_of::<read_data_block_size_rp>(), 7);
    assert_eq!(align_of::<read_data_block_size_rp>(), 1);
    assert_eq!(size_of::<read_failed_contact_counter_rp>(), 4);
    assert_eq!(align_of::<read_failed_contact_counter_rp>(), 1);
    assert_eq!(size_of::<reset_failed_contact_counter_rp>(), 3);
    assert_eq!(align_of::<reset_failed_contact_counter_rp>(), 1);
    assert_eq!(size_of::<read_link_quality_rp>(), 4);
    assert_eq!(align_of::<read_link_quality_rp>(), 1);
    assert_eq!(size_of::<read_rssi_rp>(), 4);
    assert_eq!(align_of::<read_rssi_rp>(), 1);
    assert_eq!(size_of::<read_afh_map_rp>(), 14);
    assert_eq!(align_of::<read_afh_map_rp>(), 1);
    assert_eq!(size_of::<read_clock_cp>(), 3);
    assert_eq!(align_of::<read_clock_cp>(), 1);
    assert_eq!(size_of::<read_clock_rp>(), 9);
    assert_eq!(align_of::<read_clock_rp>(), 1);
    assert_eq!(size_of::<read_local_amp_info_rp>(), 31);
    assert_eq!(align_of::<read_local_amp_info_rp>(), 1);
    assert_eq!(size_of::<read_local_amp_assoc_cp>(), 5);
    assert_eq!(align_of::<read_local_amp_assoc_cp>(), 1);
    assert_eq!(size_of::<read_local_amp_assoc_rp>(), 252);
    assert_eq!(align_of::<read_local_amp_assoc_rp>(), 1);
    assert_eq!(size_of::<write_remote_amp_assoc_cp>(), 253);
    assert_eq!(align_of::<write_remote_amp_assoc_cp>(), 1);
    assert_eq!(size_of::<write_remote_amp_assoc_rp>(), 2);
    assert_eq!(align_of::<write_remote_amp_assoc_rp>(), 1);
    assert_eq!(size_of::<write_simple_pairing_debug_mode_cp>(), 1);
    assert_eq!(align_of::<write_simple_pairing_debug_mode_cp>(), 1);
    assert_eq!(size_of::<write_simple_pairing_debug_mode_rp>(), 1);
    assert_eq!(align_of::<write_simple_pairing_debug_mode_rp>(), 1);
    assert_eq!(size_of::<le_set_event_mask_cp>(), 8);
    assert_eq!(align_of::<le_set_event_mask_cp>(), 1);
    assert_eq!(size_of::<le_read_buffer_size_rp>(), 4);
    assert_eq!(align_of::<le_read_buffer_size_rp>(), 1);
    assert_eq!(size_of::<le_read_local_supported_features_rp>(), 9);
    assert_eq!(align_of::<le_read_local_supported_features_rp>(), 1);
    assert_eq!(size_of::<le_set_random_address_cp>(), 6);
    assert_eq!(align_of::<le_set_random_address_cp>(), 1);
    assert_eq!(size_of::<le_set_advertising_parameters_cp>(), 15);
    assert_eq!(align_of::<le_set_advertising_parameters_cp>(), 1);
    assert_eq!(size_of::<le_read_advertising_channel_tx_power_rp>(), 2);
    assert_eq!(align_of::<le_read_advertising_channel_tx_power_rp>(), 1);
    assert_eq!(size_of::<le_set_advertising_data_cp>(), 32);
    assert_eq!(align_of::<le_set_advertising_data_cp>(), 1);
    assert_eq!(size_of::<le_set_scan_response_data_cp>(), 32);
    assert_eq!(align_of::<le_set_scan_response_data_cp>(), 1);
    assert_eq!(size_of::<le_set_advertise_enable_cp>(), 1);
    assert_eq!(align_of::<le_set_advertise_enable_cp>(), 1);
    assert_eq!(size_of::<le_set_scan_parameters_cp>(), 7);
    assert_eq!(align_of::<le_set_scan_parameters_cp>(), 1);
    assert_eq!(size_of::<le_set_scan_enable_cp>(), 2);
    assert_eq!(align_of::<le_set_scan_enable_cp>(), 1);
    assert_eq!(size_of::<le_create_connection_cp>(), 25);
    assert_eq!(align_of::<le_create_connection_cp>(), 1);
    assert_eq!(size_of::<le_read_white_list_size_rp>(), 2);
    assert_eq!(align_of::<le_read_white_list_size_rp>(), 1);
    assert_eq!(size_of::<le_add_device_to_white_list_cp>(), 7);
    assert_eq!(align_of::<le_add_device_to_white_list_cp>(), 1);
    assert_eq!(size_of::<le_remove_device_from_white_list_cp>(), 7);
    assert_eq!(align_of::<le_remove_device_from_white_list_cp>(), 1);
    assert_eq!(size_of::<le_connection_update_cp>(), 14);
    assert_eq!(align_of::<le_connection_update_cp>(), 1);
    assert_eq!(size_of::<le_set_host_channel_classification_cp>(), 5);
    assert_eq!(align_of::<le_set_host_channel_classification_cp>(), 1);
    assert_eq!(size_of::<le_read_channel_map_cp>(), 2);
    assert_eq!(align_of::<le_read_channel_map_cp>(), 1);
    assert_eq!(size_of::<le_read_channel_map_rp>(), 8);
    assert_eq!(align_of::<le_read_channel_map_rp>(), 1);
    assert_eq!(size_of::<le_read_remote_used_features_cp>(), 2);
    assert_eq!(align_of::<le_read_remote_used_features_cp>(), 1);
    assert_eq!(size_of::<le_encrypt_cp>(), 32);
    assert_eq!(align_of::<le_encrypt_cp>(), 1);
    assert_eq!(size_of::<le_encrypt_rp>(), 17);
    assert_eq!(align_of::<le_encrypt_rp>(), 1);
    assert_eq!(size_of::<le_rand_rp>(), 9);
    assert_eq!(align_of::<le_rand_rp>(), 1);
    assert_eq!(size_of::<le_start_encryption_cp>(), 28);
    assert_eq!(align_of::<le_start_encryption_cp>(), 1);
    assert_eq!(size_of::<le_ltk_reply_cp>(), 18);
    assert_eq!(align_of::<le_ltk_reply_cp>(), 1);
    assert_eq!(size_of::<le_ltk_reply_rp>(), 3);
    assert_eq!(align_of::<le_ltk_reply_rp>(), 1);
    assert_eq!(size_of::<le_ltk_neg_reply_cp>(), 2);
    assert_eq!(align_of::<le_ltk_neg_reply_cp>(), 1);
    assert_eq!(size_of::<le_ltk_neg_reply_rp>(), 3);
    assert_eq!(align_of::<le_ltk_neg_reply_rp>(), 1);
    assert_eq!(size_of::<le_read_supported_states_rp>(), 9);
    assert_eq!(align_of::<le_read_supported_states_rp>(), 1);
    assert_eq!(size_of::<le_receiver_test_cp>(), 1);
    assert_eq!(align_of::<le_receiver_test_cp>(), 1);
    assert_eq!(size_of::<le_transmitter_test_cp>(), 3);
    assert_eq!(align_of::<le_transmitter_test_cp>(), 1);
    assert_eq!(size_of::<le_test_end_rp>(), 3);
    assert_eq!(align_of::<le_test_end_rp>(), 1);
    assert_eq!(size_of::<le_add_device_to_resolv_list_cp>(), 39);
    assert_eq!(align_of::<le_add_device_to_resolv_list_cp>(), 1);
    assert_eq!(size_of::<le_remove_device_from_resolv_list_cp>(), 7);
    assert_eq!(align_of::<le_remove_device_from_resolv_list_cp>(), 1);
    assert_eq!(size_of::<le_read_resolv_list_size_rp>(), 2);
    assert_eq!(align_of::<le_read_resolv_list_size_rp>(), 1);
    assert_eq!(size_of::<le_set_address_resolution_enable_cp>(), 1);
    assert_eq!(align_of::<le_set_address_resolution_enable_cp>(), 1);
    assert_eq!(size_of::<inquiry_info>(), 14);
    assert_eq!(align_of::<inquiry_info>(), 1);
    assert_eq!(size_of::<evt_conn_complete>(), 11);
    assert_eq!(align_of::<evt_conn_complete>(), 1);
    assert_eq!(size_of::<evt_conn_request>(), 10);
    assert_eq!(align_of::<evt_conn_request>(), 1);
    assert_eq!(size_of::<evt_disconn_complete>(), 4);
    assert_eq!(align_of::<evt_disconn_complete>(), 1);
    assert_eq!(size_of::<evt_auth_complete>(), 3);
    assert_eq!(align_of::<evt_auth_complete>(), 1);
    assert_eq!(size_of::<evt_remote_name_req_complete>(), 255);
    assert_eq!(align_of::<evt_remote_name_req_complete>(), 1);
    assert_eq!(size_of::<evt_encrypt_change>(), 4);
    assert_eq!(align_of::<evt_encrypt_change>(), 1);
    assert_eq!(size_of::<evt_change_conn_link_key_complete>(), 3);
    assert_eq!(align_of::<evt_change_conn_link_key_complete>(), 1);
    assert_eq!(size_of::<evt_master_link_key_complete>(), 4);
    assert_eq!(align_of::<evt_master_link_key_complete>(), 1);
    assert_eq!(size_of::<evt_read_remote_features_complete>(), 11);
    assert_eq!(align_of::<evt_read_remote_features_complete>(), 1);
    assert_eq!(size_of::<evt_read_remote_version_complete>(), 8);
    assert_eq!(align_of::<evt_read_remote_version_complete>(), 1);
    assert_eq!(size_of::<evt_qos_setup_complete>(), 21);
    assert_eq!(align_of::<evt_qos_setup_complete>(), 1);
    assert_eq!(size_of::<evt_cmd_complete>(), 3);
    assert_eq!(align_of::<evt_cmd_complete>(), 1);
    assert_eq!(size_of::<evt_cmd_status>(), 4);
    assert_eq!(align_of::<evt_cmd_status>(), 1);
    assert_eq!(size_of::<evt_hardware_error>(), 1);
    assert_eq!(align_of::<evt_hardware_error>(), 1);
    assert_eq!(size_of::<evt_flush_occured>(), 2);
    assert_eq!(align_of::<evt_flush_occured>(), 1);
    assert_eq!(size_of::<evt_role_change>(), 8);
    assert_eq!(align_of::<evt_role_change>(), 1);
    assert_eq!(size_of::<evt_num_comp_pkts>(), 1);
    assert_eq!(align_of::<evt_num_comp_pkts>(), 1);
    assert_eq!(size_of::<evt_mode_change>(), 6);
    assert_eq!(align_of::<evt_mode_change>(), 1);
    assert_eq!(size_of::<evt_return_link_keys>(), 1);
    assert_eq!(align_of::<evt_return_link_keys>(), 1);
    assert_eq!(size_of::<evt_pin_code_req>(), 6);
    assert_eq!(align_of::<evt_pin_code_req>(), 1);
    assert_eq!(size_of::<evt_link_key_req>(), 6);
    assert_eq!(align_of::<evt_link_key_req>(), 1);
    assert_eq!(size_of::<evt_link_key_notify>(), 23);
    assert_eq!(align_of::<evt_link_key_notify>(), 1);
    assert_eq!(size_of::<evt_data_buffer_overflow>(), 1);
    assert_eq!(align_of::<evt_data_buffer_overflow>(), 1);
    assert_eq!(size_of::<evt_max_slots_change>(), 3);
    assert_eq!(align_of::<evt_max_slots_change>(), 1);
    assert_eq!(size_of::<evt_read_clock_offset_complete>(), 5);
    assert_eq!(align_of::<evt_read_clock_offset_complete>(), 1);
    assert_eq!(size_of::<evt_conn_ptype_changed>(), 5);
    assert_eq!(align_of::<evt_conn_ptype_changed>(), 1);
    assert_eq!(size_of::<evt_qos_violation>(), 2);
    assert_eq!(align_of::<evt_qos_violation>(), 1);
    assert_eq!(size_of::<evt_pscan_rep_mode_change>(), 7);
    assert_eq!(align_of::<evt_pscan_rep_mode_change>(), 1);
    assert_eq!(size_of::<evt_flow_spec_complete>(), 22);
    assert_eq!(align_of::<evt_flow_spec_complete>(), 1);
    assert_eq!(size_of::<inquiry_info_with_rssi>(), 14);
    assert_eq!(align_of::<inquiry_info_with_rssi>(), 1);
    assert_eq!(size_of::<inquiry_info_with_rssi_and_pscan_mode>(), 15);
    assert_eq!(align_of::<inquiry_info_with_rssi_and_pscan_mode>(), 1);
    assert_eq!(size_of::<evt_read_remote_ext_features_complete>(), 13);
    assert_eq!(align_of::<evt_read_remote_ext_features_complete>(), 1);
    assert_eq!(size_of::<evt_sync_conn_complete>(), 17);
    assert_eq!(align_of::<evt_sync_conn_complete>(), 1);
    assert_eq!(size_of::<evt_sync_conn_changed>(), 9);
    assert_eq!(align_of::<evt_sync_conn_changed>(), 1);
    assert_eq!(size_of::<evt_sniff_subrating>(), 11);
    assert_eq!(align_of::<evt_sniff_subrating>(), 1);
    assert_eq!(size_of::<extended_inquiry_info>(), 254);
    assert_eq!(align_of::<extended_inquiry_info>(), 1);
    assert_eq!(size_of::<evt_encryption_key_refresh_complete>(), 3);
    assert_eq!(align_of::<evt_encryption_key_refresh_complete>(), 1);
    assert_eq!(size_of::<evt_io_capability_request>(), 6);
    assert_eq!(align_of::<evt_io_capability_request>(), 1);
    assert_eq!(size_of::<evt_io_capability_response>(), 9);
    assert_eq!(align_of::<evt_io_capability_response>(), 1);
    assert_eq!(size_of::<evt_user_confirm_request>(), 10);
    assert_eq!(align_of::<evt_user_confirm_request>(), 1);
    assert_eq!(size_of::<evt_user_passkey_request>(), 6);
    assert_eq!(align_of::<evt_user_passkey_request>(), 1);
    assert_eq!(size_of::<evt_remote_oob_data_request>(), 6);
    assert_eq!(align_of::<evt_remote_oob_data_request>(), 1);
    assert_eq!(size_of::<evt_simple_pairing_complete>(), 7);
    assert_eq!(align_of::<evt_simple_pairing_complete>(), 1);
    assert_eq!(size_of::<evt_link_supervision_timeout_changed>(), 4);
    assert_eq!(align_of::<evt_link_supervision_timeout_changed>(), 1);
    assert_eq!(size_of::<evt_enhanced_flush_complete>(), 2);
    assert_eq!(align_of::<evt_enhanced_flush_complete>(), 1);
    assert_eq!(size_of::<evt_user_passkey_notify>(), 10);
    assert_eq!(align_of::<evt_user_passkey_notify>(), 1);
    assert_eq!(size_of::<evt_keypress_notify>(), 7);
    assert_eq!(align_of::<evt_keypress_notify>(), 1);
    assert_eq!(size_of::<evt_remote_host_features_notify>(), 14);
    assert_eq!(align_of::<evt_remote_host_features_notify>(), 1);
    assert_eq!(size_of::<evt_le_meta_event>(), 1);
    assert_eq!(align_of::<evt_le_meta_event>(), 1);
    assert_eq!(size_of::<evt_le_connection_complete>(), 18);
    assert_eq!(align_of::<evt_le_connection_complete>(), 1);
    assert_eq!(size_of::<le_advertising_info>(), 9);
    assert_eq!(align_of::<le_advertising_info>(), 1);
    assert_eq!(size_of::<evt_le_connection_update_complete>(), 9);
    assert_eq!(align_of::<evt_le_connection_update_complete>(), 1);
    assert_eq!(size_of::<evt_le_read_remote_used_features_complete>(), 11);
    assert_eq!(align_of::<evt_le_read_remote_used_features_complete>(), 1);
    assert_eq!(size_of::<evt_le_long_term_key_request>(), 12);
    assert_eq!(align_of::<evt_le_long_term_key_request>(), 1);
    assert_eq!(size_of::<evt_physical_link_complete>(), 2);
    assert_eq!(align_of::<evt_physical_link_complete>(), 1);
    assert_eq!(size_of::<evt_disconn_physical_link_complete>(), 3);
    assert_eq!(align_of::<evt_disconn_physical_link_complete>(), 1);
    assert_eq!(size_of::<evt_physical_link_loss_warning>(), 2);
    assert_eq!(align_of::<evt_physical_link_loss_warning>(), 1);
    assert_eq!(size_of::<evt_physical_link_recovery>(), 1);
    assert_eq!(align_of::<evt_physical_link_recovery>(), 1);
    assert_eq!(size_of::<evt_logical_link_complete>(), 5);
    assert_eq!(align_of::<evt_logical_link_complete>(), 1);
    assert_eq!(size_of::<evt_flow_spec_modify_complete>(), 3);
    assert_eq!(align_of::<evt_flow_spec_modify_complete>(), 1);
    assert_eq!(size_of::<cmplt_handle>(), 6);
    assert_eq!(align_of::<cmplt_handle>(), 1);
    assert_eq!(size_of::<evt_num_completed_blocks>(), 3);
    assert_eq!(align_of::<evt_num_completed_blocks>(), 1);
    assert_eq!(size_of::<evt_amp_status_change>(), 2);
    assert_eq!(align_of::<evt_amp_status_change>(), 1);
    assert_eq!(size_of::<evt_stack_internal>(), 2);
    assert_eq!(align_of::<evt_stack_internal>(), 1);
    assert_eq!(size_of::<evt_si_device>(), 4);
    assert_eq!(align_of::<evt_si_device>(), 1);
    assert_eq!(size_of::<hci_command_hdr>(), 3);
    assert_eq!(align_of::<hci_command_hdr>(), 1);
    assert_eq!(size_of::<hci_event_hdr>(), 2);
    assert_eq!(align_of::<hci_event_hdr>(), 1);
    assert_eq!(size_of::<hci_acl_hdr>(), 4);
    assert_eq!(align_of::<hci_acl_hdr>(), 1);
    assert_eq!(size_of::<hci_sco_hdr>(), 3);
    assert_eq!(align_of::<hci_sco_hdr>(), 1);
    assert_eq!(size_of::<hci_msg_hdr>(), 6);
    assert_eq!(align_of::<hci_msg_hdr>(), 1);
    assert_eq!(size_of::<sockaddr_hci>(), 6);
    assert_eq!(align_of::<sockaddr_hci>(), 2);
    assert_eq!(size_of::<hci_filter>(), 16);
    assert_eq!(align_of::<hci_filter>(), 4);
    assert_eq!(size_of::<hci_dev_stats>(), 40);
    assert_eq!(align_of::<hci_dev_stats>(), 4);
    assert_eq!(size_of::<hci_dev_info>(), 92);
    assert_eq!(align_of::<hci_dev_info>(), 4);
    assert_eq!(size_of::<hci_conn_info>(), 16);
    assert_eq!(align_of::<hci_conn_info>(), 4);
    assert_eq!(size_of::<hci_dev_req>(), 8);
    assert_eq!(align_of::<hci_dev_req>(), 4);
    assert_eq!(size_of::<hci_dev_list_req>(), 4);
    assert_eq!(align_of::<hci_dev_list_req>(), 4);
    assert_eq!(size_of::<hci_conn_list_req>(), 4);
    assert_eq!(align_of::<hci_conn_list_req>(), 4);
    assert_eq!(size_of::<hci_conn_info_req>(), 8);
    assert_eq!(align_of::<hci_conn_info_req>(), 4);
    assert_eq!(size_of::<hci_auth_info_req>(), 7);
    assert_eq!(align_of::<hci_auth_info_req>(), 1);
    assert_eq!(size_of::<hci_inquiry_req>(), 10);
    assert_eq!(align_of::<hci_inquiry_req>(), 2);
}
#[test]
fn hci_lib() {
    use libbluetooth::hci_lib::*;
    assert_eq!(size_of::<hci_request>(), 40);
    assert_eq!(align_of::<hci_request>(), 8);
    assert_eq!(size_of::<hci_version>(), 10);
    assert_eq!(align_of::<hci_version>(), 2);
}
#[test]
fn l2cap() {
    use libbluetooth::l2cap::*;
    assert_eq!(size_of::<sockaddr_l2>(), 14);
    assert_eq!(align_of::<sockaddr_l2>(), 2);
    assert_eq!(size_of::<l2cap_options>(), 12);
    assert_eq!(align_of::<l2cap_options>(), 2);
    assert_eq!(size_of::<l2cap_conninfo>(), 6);
    assert_eq!(align_of::<l2cap_conninfo>(), 2);
    assert_eq!(size_of::<l2cap_hdr>(), 4);
    assert_eq!(align_of::<l2cap_hdr>(), 1);
    assert_eq!(size_of::<l2cap_cmd_hdr>(), 4);
    assert_eq!(align_of::<l2cap_cmd_hdr>(), 1);
    assert_eq!(size_of::<l2cap_cmd_rej>(), 2);
    assert_eq!(align_of::<l2cap_cmd_rej>(), 1);
    assert_eq!(size_of::<l2cap_conn_req>(), 4);
    assert_eq!(align_of::<l2cap_conn_req>(), 1);
    assert_eq!(size_of::<l2cap_conn_rsp>(), 8);
    assert_eq!(align_of::<l2cap_conn_rsp>(), 1);
    assert_eq!(size_of::<l2cap_conf_req>(), 4);
    assert_eq!(align_of::<l2cap_conf_req>(), 1);
    assert_eq!(size_of::<l2cap_conf_rsp>(), 6);
    assert_eq!(align_of::<l2cap_conf_rsp>(), 1);
    assert_eq!(size_of::<l2cap_conf_opt>(), 2);
    assert_eq!(align_of::<l2cap_conf_opt>(), 1);
    assert_eq!(size_of::<l2cap_disconn_req>(), 4);
    assert_eq!(align_of::<l2cap_disconn_req>(), 1);
    assert_eq!(size_of::<l2cap_disconn_rsp>(), 4);
    assert_eq!(align_of::<l2cap_disconn_rsp>(), 1);
    assert_eq!(size_of::<l2cap_info_req>(), 2);
    assert_eq!(align_of::<l2cap_info_req>(), 1);
    assert_eq!(size_of::<l2cap_info_rsp>(), 4);
    assert_eq!(align_of::<l2cap_info_rsp>(), 1);
    assert_eq!(size_of::<l2cap_create_req>(), 5);
    assert_eq!(align_of::<l2cap_create_req>(), 1);
    assert_eq!(size_of::<l2cap_create_rsp>(), 8);
    assert_eq!(align_of::<l2cap_create_rsp>(), 1);
    assert_eq!(size_of::<l2cap_move_req>(), 3);
    assert_eq!(align_of::<l2cap_move_req>(), 1);
    assert_eq!(size_of::<l2cap_move_rsp>(), 4);
    assert_eq!(align_of::<l2cap_move_rsp>(), 1);
    assert_eq!(size_of::<l2cap_move_cfm>(), 4);
    assert_eq!(align_of::<l2cap_move_cfm>(), 1);
    assert_eq!(size_of::<l2cap_move_cfm_rsp>(), 2);
    assert_eq!(align_of::<l2cap_move_cfm_rsp>(), 1);
}
#[test]
fn rfcomm() {
    use libbluetooth::rfcomm::*;
    assert_eq!(size_of::<sockaddr_rc>(), 10);
    assert_eq!(align_of::<sockaddr_rc>(), 2);
    assert_eq!(size_of::<rfcomm_conninfo>(), 6);
    assert_eq!(align_of::<rfcomm_conninfo>(), 2);
    assert_eq!(size_of::<rfcomm_dev_req>(), 24);
    assert_eq!(align_of::<rfcomm_dev_req>(), 4);
    assert_eq!(size_of::<rfcomm_dev_info>(), 24);
    assert_eq!(align_of::<rfcomm_dev_info>(), 4);
    assert_eq!(size_of::<rfcomm_dev_list_req>(), 4);
    assert_eq!(align_of::<rfcomm_dev_list_req>(), 4);
}
#[test]
fn sco() {
    use libbluetooth::sco::*;
    assert_eq!(size_of::<sockaddr_sco>(), 8);
    assert_eq!(align_of::<sockaddr_sco>(), 2);
    assert_eq!(size_of::<sco_options>(), 2);
    assert_eq!(align_of::<sco_options>(), 2);
    assert_eq!(size_of::<sco_conninfo>(), 6);
    assert_eq!(align_of::<sco_conninfo>(), 2);
}
#[test]
fn sdp() {
    use libbluetooth::sdp::*;
    assert_eq!(size_of::<sdp_pdu_hdr_t>(), 5);
    assert_eq!(align_of::<sdp_pdu_hdr_t>(), 1);
    assert_eq!(size_of::<uuid_t>(), 20);
    assert_eq!(align_of::<uuid_t>(), 4);
    assert_eq!(size_of::<_sdp_list>(), 16);
    assert_eq!(align_of::<_sdp_list>(), 8);
    assert_eq!(size_of::<sdp_lang_attr_t>(), 6);
    assert_eq!(align_of::<sdp_lang_attr_t>(), 2);
    assert_eq!(size_of::<sdp_profile_desc_t>(), 24);
    assert_eq!(align_of::<sdp_profile_desc_t>(), 4);
    assert_eq!(size_of::<sdp_version_t>(), 2);
    assert_eq!(align_of::<sdp_version_t>(), 1);
    assert_eq!(size_of::<sdp_buf_t>(), 16);
    assert_eq!(align_of::<sdp_buf_t>(), 8);
    assert_eq!(size_of::<sdp_record_t>(), 48);
    assert_eq!(align_of::<sdp_record_t>(), 8);
    assert_eq!(size_of::<sdp_data_struct>(), 48);
    assert_eq!(align_of::<sdp_data_struct>(), 8);
}
#[test]
fn sdp_lib() {
    use libbluetooth::sdp_lib::*;
    assert_eq!(size_of::<sdp_session_t>(), 32);
    assert_eq!(align_of::<sdp_session_t>(), 8);
}
